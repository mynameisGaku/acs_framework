#pragma once

// =============================================================================
// プリコンパイル済みヘッダー (PCH)
// -----------------------------------------------------------------------------
// ACS の単一ヘッダー <acs.h> は約 9.6 万行 / 3.5MB あり、翻訳単位ごとに解析すると
// ビルド時間を大きく押し上げる。ここで一度だけ事前コンパイルし、全 TU で共有する。
//
// vcxproj の ForcedIncludeFiles により本ヘッダーは全 TU へ自動注入されるため、
// 各ソースが個別に #include "Common/Pch.h" と書く必要はない。
// (既存ソースの #include <acs.h> は #pragma once により二重取り込みにならない)
//
// 追加するのは「ほぼ変更されない」ヘッダーのみに限ること。ここへ入れたヘッダーを
// 編集するとプロジェクト全体が再コンパイルされる。
// =============================================================================

#include <acs.h>
