// SPDX-License-Identifier: Apache-2.0
#pragma once

/**
 * @file
 * 印だけを付けるマクロ。配布物の世代差をここで吸収する。
 *
 * @details
 * `ACS_ENUM()` は「この列挙型は反映の対象」という印。新しい ACS はこの印を見て
 * 列挙子の名前表を作るが、手元の配布物 (2026-08-03 生成) はこの印を知らない。
 * 知らない配布物では**何もしないもの**として畳む。
 *
 * すでに定義されているときは触らない。エンジンが本物を用意している場に
 * 空の定義をかぶせると、名前表が作られなくなって画面から選択肢が黙って消える。
 */

#if !defined( ACS_ENUM )
	/** 反映の対象という印 (この配布物では何もしない)。 */
	#define ACS_ENUM( ... )
#endif
